# 恋爱纪念日网页 PRD

## 1. 文档信息
- 产品名称：与你的每一天（恋爱纪念日单页）
- 文档版本：v1.0（基于当前实现反向整理）
- 文档目的：明确当前产品的功能范围、交互要求、视觉基调、技术约束与验收标准，便于后续维护与迭代。

## 2. 产品背景
这是一个以“恋爱纪念日”为核心主题的单页网页，用于展示双方在一起的累计时长，并通过文案、氛围动画和互动桌宠强化情绪表达。  
当前项目为纯前端静态页面，所有逻辑集中在 `index.html` 中，无构建流程、无外部依赖管理。

## 3. 产品目标
1. 实时展示从固定起始日（2026-01-25，北京时间）至今的恋爱时长。
2. 通过柔和浪漫视觉风格提供“可长期驻留”的纪念页面体验。
3. 增加轻互动元素（随机金句、桌宠反馈）提升陪伴感和趣味性。
4. 保持实现简单、可直接本地打开，便于长期保存与低成本维护。

## 4. 目标用户与使用场景
### 4.1 目标用户
- 有纪念日展示需求的情侣用户。
- 偏好轻量、无需部署和登录的静态网页使用者。

### 4.2 典型场景
- 在纪念日当天查看“在一起第 N 天”。
- 日常打开页面获取情绪价值（文案+动画+桌宠互动）。
- 在手机与桌面端查看同一页面内容。

## 5. 产品范围
### 5.1 当前版本范围（In Scope）
- 恋爱天数与时分秒实时计数。
- 北京时间展示。
- 随机恋爱金句与“运势标签”展示。
- 背景花瓣动画。
- 可拖拽互动猫咪桌宠（包含多状态动画、点击爱心反馈）。
- 响应式适配（移动端基础适配）。

### 5.2 非当前范围（Out of Scope）
- 后端服务与账号系统。
- 数据持久化与多用户配置。
- 多页面路由与复杂内容管理后台。
- 国际化多语言切换。

## 6. 功能需求
### 6.1 实时纪念日计时器
**需求描述**
- 页面需展示“我们在一起的第 N 天”。
- 同时展示小时、分钟、秒的细分计时。
- 计时每秒更新一次。

**规则要求**
- 起始时间固定为 2026-01-25 00:00:00（北京时间）。
- 前端需正确处理本地时区差异，保证北京时间展示一致。
- 若当前时间早于起始时间，计时不显示负数（保持初始状态）。

**展示要求**
- Header 与主卡片中的天数需保持同步。
- 小时/分钟/秒需补零显示为两位数。

### 6.2 北京时间显示
**需求描述**
- 页脚展示当前北京时间，格式统一可读。

**规则要求**
- 以“北京时间 YYYY.MM.DD HH:mm:ss”格式实时刷新（1 秒）。
- 时间计算应独立于系统本地时区，统一按 UTC+8 处理。

### 6.3 今日金句与运势标签
**需求描述**
- 页面加载时从预置语录池随机抽取 1 条文案展示。
- 同步显示对应运势标签与图标。

**规则要求**
- 当前实现为单次加载随机，不要求定时轮播。
- 文案语言为中文，风格需保持温柔、积极、恋爱主题一致。

### 6.4 氛围花瓣动画
**需求描述**
- 页面全局展示漂浮花瓣动画，增强浪漫氛围。

**规则要求**
- 花瓣数量随屏幕宽度分档（移动端更少，桌面端更多）。
- 每片花瓣随机尺寸、速度、延迟、漂移和透明度峰值。
- 花瓣层不可拦截用户交互（pointer-events: none）。

### 6.5 桌面宠物猫互动
**需求描述**
- 页面固定叠加一只可互动猫咪（Canvas 绘制）。

**交互要求**
- 猫咪眼睛跟随鼠标/触摸位置变化。
- 支持鼠标拖拽移动位置。
- 点击（非拖拽）触发被抚摸反应并生成爱心动画。
- 触摸端支持 touchstart/touchmove/touchend 交互。

**状态行为要求**
- 猫咪包含 sit/stretch/lick/roll/yawn/sleep/walk 等状态机行为。
- 各状态按随机时长轮播切换。
- sleep 状态可生成漂浮 “z” 字动画。
- walk 状态在屏幕边界范围内来回移动。

### 6.6 响应式与视觉排版
**需求描述**
- 在移动端（<=480px）保持可读性与主功能完整。

**规则要求**
- 计时卡片内边距、分隔符、文案间距按小屏优化。
- 小屏可隐藏部分装饰性分隔元素（如时间冒号）以提升布局稳定性。

## 7. 视觉与品牌调性需求
1. 整体风格：浪漫、柔和、精致、轻复古。
2. 主色体系：ivory/rose/gold/burgundy（通过 CSS 变量统一管理）。
3. 字体风格：中英混排衬线气质，强调情感表达与仪式感。
4. 动效原则：慢节奏、轻量、不干扰核心信息读取。

## 8. 非功能需求
### 8.1 性能
- 页面首次加载应快速可见（单文件静态资源）。
- 动画与计时在常见设备上保持流畅（目标 30fps+，桌面优先接近 60fps）。

### 8.2 兼容性
- 支持现代 Chromium 内核浏览器（桌面与移动）。
- 页面可直接本地打开（file://）运行。

### 8.3 可维护性
- 项目保持单文件结构，便于快速分发。
- 样式颜色统一由 `:root` 变量控制，降低改色成本。
- 关键时间计算逻辑需保留注释与常量命名清晰性。

## 9. 技术约束与实现约定
1. 当前项目不引入构建工具、不引入包管理器。
2. 不拆分多文件时，新增功能需保持结构清晰（style/html/script 分段明确）。
3. 起始时间常量与 UTC 偏移计算逻辑属于核心逻辑，改动需谨慎验证。
4. 语言基调以中文为主，英文仅作装饰性标签。

## 10. 验收标准（UAT）
### 10.1 核心功能验收
- 打开页面后 3 秒内可看到完整主界面与当前计时。
- 天数、时分秒每秒稳定刷新且数值递增正确。
- 页脚北京时间与真实北京时间误差 <= 1 秒（考虑刷新节奏）。
- 每次刷新页面可显示一条随机金句、标签与图标。
- 桌宠可拖拽；点击后能出现互动反馈（爱心/动作）。

### 10.2 体验验收
- 移动端 480px 宽度下无核心信息遮挡。
- 花瓣与桌宠动画不阻塞页面基础阅读与交互。
- 整体视觉风格统一，无明显突兀配色或字体风格冲突。

## 11. 后续迭代建议（非本期强制）
1. 增加“起始日期可配置”能力（本地存储）。
2. 新增“每日自动换句”而非仅刷新换句。
3. 提供“纪念日主题切换”（如春日、夜空、节日限定）。
4. 增加桌宠开关与性能模式（低性能设备可关闭复杂动画）。

